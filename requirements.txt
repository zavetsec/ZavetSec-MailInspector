# ZavetSec-MailInspector — зависимости
# ---------------------------------------------------------------------------
# Анализ .eml работает ТОЛЬКО на стандартной библиотеке Python.
# Пакеты ниже разблокируют полный функционал и рекомендуются для SOC.
# При отсутствии любого из них инструмент деградирует мягко и сообщает, что пропустил.
# ---------------------------------------------------------------------------

extract-msg>=0.45     # разбор Outlook .msg
oletools>=0.60        # детект VBA-макросов в Office-вложениях (olevba)
requests>=2.28        # опциональный онлайн threat-intel (--online)

# Опционально — улучшает покрытие RAR (детект RAR4 работает и без неё):
# rarfile>=4.0

# Опционально — рекурсия в 7-Zip архивы:
# py7zr>=0.20
